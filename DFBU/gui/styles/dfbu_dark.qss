/*
 * DFBU Dark Theme
 *
 * Comprehensive QSS stylesheet for the DFBU desktop application (dark mode).
 * Color values are derived from the brand palette defined in
 * DFBU/gui/theme.py (DFBUColors), with the neutral scale inverted.
 *
 * Dark Palette Reference (inverted neutral scale):
 *   Primary Blue:    #2563EB
 *   Primary Dark:    #1D4ED8
 *   Primary Light:   #3B82F6
 *   Success Green:   #10B981
 *   Warning Amber:   #F59E0B
 *   Critical Red:    #EF4444
 *   Background:      #0F172A  (was #FFFFFF)
 *   Surface:         #1E293B  (was #F8FAFC)
 *   Borders:         #334155  (was #E2E8F0)
 *   Text Primary:    #E2E8F0  (was #1E293B)
 *   Text Secondary:  #94A3B8  (was #475569)
 *   Text Disabled:   #64748B  (was #94A3B8)
 */


/* ==========================================================================
   1. Base — QMainWindow & QWidget
   ========================================================================== */

QMainWindow,
QWidget {
    background-color: #0F172A;
    font-family: "Segoe UI", "Ubuntu", "Cantarell", sans-serif;
    font-size: 13px;
    color: #E2E8F0;
}


/* ==========================================================================
   2. Tabs — QTabWidget & QTabBar
   ========================================================================== */

QTabWidget::pane {
    border: none;
    background-color: #0F172A;
}

QTabBar::tab {
    padding: 12px;
    border: none;
    background-color: transparent;
    color: #94A3B8;
}

QTabBar::tab:selected {
    border-bottom: 3px solid #2563EB;
    font-weight: bold;
    color: #3B82F6;
}

QTabBar::tab:hover:!selected {
    background-color: rgba(37, 99, 235, 0.15);
}


/* ==========================================================================
   3. Buttons — QPushButton (default, primary, danger)
   ========================================================================== */

/* Default button */
QPushButton {
    background-color: #1E293B;
    border: 1px solid #334155;
    border-radius: 6px;
    padding: 8px 16px;
    color: #E2E8F0;
}

QPushButton:hover {
    background-color: #334155;
}

QPushButton:pressed {
    background-color: #475569;
}

QPushButton:disabled {
    opacity: 0.5;
    color: #64748B;
}

/* Primary action buttons (start/Start) */
QPushButton[objectName*="start"],
QPushButton[objectName*="Start"] {
    background-color: #2563EB;
    border: 1px solid #2563EB;
    color: #FFFFFF;
}

QPushButton[objectName*="start"]:hover,
QPushButton[objectName*="Start"]:hover {
    background-color: #1D4ED8;
    border-color: #1D4ED8;
}

QPushButton[objectName*="start"]:pressed,
QPushButton[objectName*="Start"]:pressed {
    background-color: #1D4ED8;
}

QPushButton[objectName*="start"]:disabled,
QPushButton[objectName*="Start"]:disabled {
    opacity: 0.5;
}

/* Danger buttons (remove/Remove/delete) */
QPushButton[objectName*="remove"],
QPushButton[objectName*="Remove"],
QPushButton[objectName*="delete"] {
    background-color: #EF4444;
    border: 1px solid #EF4444;
    color: #FFFFFF;
}

QPushButton[objectName*="remove"]:hover,
QPushButton[objectName*="Remove"]:hover,
QPushButton[objectName*="delete"]:hover {
    background-color: #DC2626;
    border-color: #DC2626;
}

QPushButton[objectName*="remove"]:pressed,
QPushButton[objectName*="Remove"]:pressed,
QPushButton[objectName*="delete"]:pressed {
    background-color: #DC2626;
}

QPushButton[objectName*="remove"]:disabled,
QPushButton[objectName*="Remove"]:disabled,
QPushButton[objectName*="delete"]:disabled {
    opacity: 0.5;
}


/* ==========================================================================
   4. Table — QTableWidget & QHeaderView
   ========================================================================== */

QTableWidget {
    alternate-background-color: #1E293B;
    background-color: #0F172A;
    gridline-color: #334155;
    border: 1px solid #334155;
    selection-background-color: rgba(37, 99, 235, 0.25);
    selection-color: #E2E8F0;
}

QTableWidget::item {
    padding: 6px 8px;
}

QTableWidget::item:selected {
    background-color: rgba(37, 99, 235, 0.25);
    color: #E2E8F0;
}

QHeaderView::section {
    background-color: #2563EB;
    color: #FFFFFF;
    font-weight: bold;
    padding: 8px;
    border: none;
}


/* ==========================================================================
   5. Text Inputs — QLineEdit & QSpinBox
   ========================================================================== */

QLineEdit,
QSpinBox {
    border: 1px solid #334155;
    border-radius: 4px;
    padding: 8px;
    background-color: #1E293B;
    color: #E2E8F0;
}

QLineEdit:focus,
QSpinBox:focus {
    border: 2px solid #2563EB;
    padding: 7px;
}

QLineEdit::placeholder {
    color: #64748B;
}


/* ==========================================================================
   6. Checkboxes — QCheckBox
   ========================================================================== */

QCheckBox {
    spacing: 8px;
    color: #E2E8F0;
}

QCheckBox::indicator {
    width: 16px;
    height: 16px;
    border-radius: 3px;
    border: 1px solid #475569;
    background-color: #1E293B;
}

QCheckBox::indicator:hover {
    border-color: #94A3B8;
}

QCheckBox::indicator:checked {
    background-color: #2563EB;
    border-color: #2563EB;
    image: url(none);
}


/* ==========================================================================
   7. Progress Bar — QProgressBar
   ========================================================================== */

QProgressBar {
    height: 8px;
    border-radius: 4px;
    background-color: #334155;
    border: none;
    text-align: center;
}

QProgressBar::chunk {
    background-color: #2563EB;
    border-radius: 4px;
}


/* ==========================================================================
   8. Terminal / Log — QTextEdit
   ========================================================================== */

QTextEdit {
    background-color: #020617;
    color: #E2E8F0;
    font-family: "JetBrains Mono", "Fira Code", "Consolas", monospace;
    font-size: 13px;
    border: 1px solid #1E293B;
    border-radius: 4px;
    padding: 8px;
    selection-background-color: rgba(59, 130, 246, 0.4);
    selection-color: #FFFFFF;
}


/* ==========================================================================
   9. Menus — QMenuBar & QMenu
   ========================================================================== */

QMenuBar {
    background-color: #0F172A;
    padding: 4px;
    border-bottom: 1px solid #334155;
    color: #E2E8F0;
}

QMenuBar::item {
    padding: 6px 12px;
    background-color: transparent;
    border-radius: 4px;
}

QMenuBar::item:selected {
    background-color: rgba(37, 99, 235, 0.15);
}

QMenu {
    background-color: #1E293B;
    border: 1px solid #334155;
    border-radius: 6px;
    padding: 4px 0;
}

QMenu::item {
    padding: 8px 24px;
    color: #E2E8F0;
}

QMenu::item:selected {
    background-color: #2563EB;
    color: #FFFFFF;
}

QMenu::separator {
    height: 1px;
    background-color: #334155;
    margin: 4px 8px;
}


/* ==========================================================================
   10. Status Bar — QStatusBar
   ========================================================================== */

QStatusBar {
    border-top: 1px solid #334155;
    color: #94A3B8;
    background-color: #0F172A;
    padding: 4px;
}

QStatusBar::item {
    border: none;
}


/* ==========================================================================
   11. Group Box — QGroupBox
   ========================================================================== */

QGroupBox {
    font-weight: bold;
    border: 1px solid #334155;
    border-radius: 6px;
    margin-top: 12px;
    padding-top: 8px;
    color: #E2E8F0;
}

QGroupBox::title {
    subcontrol-origin: margin;
    subcontrol-position: top left;
    padding: 0 8px;
    color: #E2E8F0;
}


/* ==========================================================================
   12. Scroll Bars — QScrollBar (vertical & horizontal)
   ========================================================================== */

/* Vertical */
QScrollBar:vertical {
    width: 8px;
    background: transparent;
    margin: 0;
}

QScrollBar::handle:vertical {
    background-color: #475569;
    border-radius: 4px;
    min-height: 30px;
}

QScrollBar::handle:vertical:hover {
    background-color: #64748B;
}

QScrollBar::add-line:vertical,
QScrollBar::sub-line:vertical {
    height: 0;
    background: none;
    border: none;
}

QScrollBar::add-page:vertical,
QScrollBar::sub-page:vertical {
    background: none;
}

/* Horizontal */
QScrollBar:horizontal {
    height: 8px;
    background: transparent;
    margin: 0;
}

QScrollBar::handle:horizontal {
    background-color: #475569;
    border-radius: 4px;
    min-width: 30px;
}

QScrollBar::handle:horizontal:hover {
    background-color: #64748B;
}

QScrollBar::add-line:horizontal,
QScrollBar::sub-line:horizontal {
    width: 0;
    background: none;
    border: none;
}

QScrollBar::add-page:horizontal,
QScrollBar::sub-page:horizontal {
    background: none;
}


/* ==========================================================================
   13. Tooltips — QToolTip
   ========================================================================== */

QToolTip {
    background-color: #1E293B;
    color: #FFFFFF;
    border-radius: 4px;
    border: 1px solid #334155;
    padding: 6px 10px;
    font-size: 12px;
}


/* ==========================================================================
   13b. Empty State — Backup tab empty state messaging
   ========================================================================== */

#emptyStateTitle {
    color: #E2E8F0;
}

#emptyStateDescription {
    color: #94A3B8;
}


/* ==========================================================================
   14. Header Bar — Application branding and quick actions
   ========================================================================== */

#headerBar {
    background-color: #1E293B;
    border-bottom: 1px solid #334155;
    min-height: 48px;
    max-height: 48px;
}

#headerTitle {
    font-size: 15px;
    font-weight: bold;
    color: #E2E8F0;
}

/* Header bar buttons - compact, icon-focused style */
#helpButton,
#aboutButton,
#themeToggleButton {
    background-color: transparent;
    border: none;
    border-radius: 4px;
    padding: 6px 10px;
    min-width: 32px;
    min-height: 32px;
}

#helpButton:hover,
#aboutButton:hover,
#themeToggleButton:hover {
    background-color: rgba(37, 99, 235, 0.15);
}

#helpButton:pressed,
#aboutButton:pressed,
#themeToggleButton:pressed {
    background-color: rgba(37, 99, 235, 0.25);
}


/* ==========================================================================
   15. Log Pane — Always-visible operation log (split view)
   ========================================================================== */

#logPane {
    background-color: #0F172A;
    border-left: 1px solid #334155;
}

#logPaneHeader {
    background-color: #1E293B;
    border-bottom: 1px solid #334155;
    padding: 8px;
}

#logPaneTitle {
    font-size: 13px;
    font-weight: bold;
    color: #E2E8F0;
}

/* Log filter buttons - compact toggle style */
#logPaneFilterAllButton,
#logPaneFilterInfoButton,
#logPaneFilterWarningButton,
#logPaneFilterErrorButton {
    background-color: transparent;
    border: 1px solid #334155;
    border-radius: 4px;
    padding: 4px 8px;
    font-size: 12px;
    color: #94A3B8;
}

#logPaneFilterAllButton:hover,
#logPaneFilterInfoButton:hover,
#logPaneFilterWarningButton:hover,
#logPaneFilterErrorButton:hover {
    background-color: #1E293B;
    border-color: #475569;
}

#logPaneFilterAllButton:checked,
#logPaneFilterInfoButton:checked,
#logPaneFilterWarningButton:checked,
#logPaneFilterErrorButton:checked {
    background-color: #2563EB;
    border-color: #2563EB;
    color: #FFFFFF;
}

/* Log pane action buttons - secondary style */
#logPaneClearButton,
#logPaneSaveButton {
    background-color: transparent;
    border: 1px solid #334155;
    border-radius: 4px;
    padding: 4px 10px;
    font-size: 12px;
    color: #94A3B8;
}

#logPaneClearButton:hover,
#logPaneSaveButton:hover {
    background-color: #1E293B;
    border-color: #475569;
}

/* Verify button in log pane - primary style */
#logPaneVerifyButton {
    background-color: #2563EB;
    border: 1px solid #2563EB;
    border-radius: 4px;
    padding: 4px 10px;
    font-size: 12px;
    color: #FFFFFF;
}

#logPaneVerifyButton:hover {
    background-color: #1D4ED8;
    border-color: #1D4ED8;
}

#logPaneVerifyButton:disabled {
    opacity: 0.5;
}

/* Log text area - inherits terminal styling from section 8 */
#logPaneBox {
    border-top-left-radius: 0;
    border-top-right-radius: 0;
}


/* ==========================================================================
   16. Splitter — Resizable divider between panes
   ========================================================================== */

QSplitter::handle {
    background-color: #334155;
}

QSplitter::handle:horizontal {
    width: 1px;
}

QSplitter::handle:hover {
    background-color: #2563EB;
}
